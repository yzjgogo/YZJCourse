<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="feedback_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>


        <item name="android:windowLightStatusBar">true</item><!--背景颜色强制设为亮色-->
        <item name="android:statusBarColor">@android:color/transparent</item><!--不设置，8.0、魅族手机为蓝色、小米为灰色-->
    </style>
</resources>